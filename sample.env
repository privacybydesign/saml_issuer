HTTP_PROTOCOL=              # 'https' or 'http'
HOST_NAME=
IRMA_SERVER_URL=
API_TOKEN=
SSP_SECRET_SALT=            # SimpleSAMLphp secret salt (required; use a long random value, e.g. `openssl rand -hex 32`)
SSP_ADMIN_PASSWORD=         # SimpleSAMLphp admin pass (required; must not be 'admin' — the container refuses to start)
LINKEDIN_CLIENT_ID=
LINKEDIN_CLIENT_SECRET=